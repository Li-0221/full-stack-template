# 必填：同一服务器上唯一的项目标识；dev/prod 使用相同标识。
PROJECT_NAME=
# 必填：当前环境密钥，至少 32 字符。
APP_SECRET_KEY=
POSTGRES_DB=app_prod
POSTGRES_USER=app_prod
# 必填：使用字母、数字、下划线或短横线，供数据库连接串使用。
POSTGRES_PASSWORD=
SITE_URL=http://example.com
FRONTEND_PORT=3000
BACKEND_PORT=8000
